## v0.1.104 — 2026-08-25

### AI 智能体

- **子任务确认卡给出的是每个智能体自己的模型，以及它真正认识的思考强度参数。** 确认卡过去不管面对哪个智能体，都用 `--model` 和 `--effort` 拼启动参数，但只有 Claude、Kiro 和 Antigravity 用这个写法——Grok 和 Zoo 叫 `--reasoning-effort`，Cline 叫 `--thinking`。于是给其余智能体选一次思考强度，等于往命令行里塞了一个它从没听说过的参数，会话直接起不来。现在每个智能体都给出自己的参数名和取值范围。模型控件也跟着各家 CLI 的实际能力走：能列出目录的（OpenCode、Grok、Crush、Antigravity、Cursor、pi、Kiro）会去问它要，给出真实列表；只有固定几个的（Claude、Codex、Kimi Code）给出那几个；剩下的给一个文本框，附上它期望的写法示例。没装或者没登录的智能体会直接说列表取不到，而不是一直转圈。选「Default」现在是把继承来的设定清掉，而不是留着旧值；选思考强度也不会再顺手把父会话传下来的模型弄丢。

- **从 Kimi Code 会话派生的子任务仍然是 Kimi Code。** 派生时用来继承父会话智能体的那张表里漏了 Kimi Code，于是它的子会话会悄悄退回默认智能体。

### 工作区

- **分组建好之后还能改绑 worktree。** 过去 worktree 只能在建分组的时候选，选完就定死，想换只能把分组删了重建。现在右键分组选「Move to Worktree…」，可以新建一个 worktree、绑到已有的某一个，已经绑过的分组则是改指向。改动只落在分组本身：分组里已有的会话保持它们创建时的目录——正在跑的会话没法在自己脚下换目录——之后新建的会话才会起在这个 worktree 里。

- **镜像模式现在覆盖整棵左树。** 它此前同步的是选中项和折叠的面板，搜索框、状态与标记过滤留在本地，理由是同步过去会打断正在查东西的人。这个理由说反了：镜像的意思是两个窗口持有同一份状态，不是一边把另一边的按键重放一遍——这边开着的过滤条件，那边也开着。真正打断人的，恰恰是两边看到的树不一样。现在左树的每个投影都跟着走：分屏结构、投影的名字、搜索词、状态与标记过滤，还有各自独立的折叠状态。快照格式升到第 2 版，版本对不上的一方会停止镜像，而不是套用半帧内容，所以升级期间一直开着的窗口刷新一下即可。

### 界面

- **macOS 上关窗口问的问题和退出一样了。** ⌘Q 和菜单里的退出都会走应用自己的确认，但红色关闭按钮是直接把窗口销毁——而承载确认对话框的 webview 就在那个窗口里。结果要么根本没有确认，要么只剩下降级的原生弹框：没有「保存工作区」勾选框，文案也没翻译。现在三个平台都会先把窗口留住，等你回答。

- **「保存工作区」默认勾选，而且改了之后记得住。** 丢掉布局的代价比多存一份用不上的快照大，所以这一项默认是勾上的。它此前还会忘事：这个设置要经过 400 毫秒防抖才落库，而退出会在这个窗口期里把进程杀掉，下次启动对账时便以旧值为准，把你的改动改了回去。现在退出前会先把这次写入刷下去，并且最多等 600 毫秒，后端卡住也不会让确认按钮一直转。（由 FarhadGSRX 贡献。）

- **远程连接面板里的密码可以看明文。** URL 密码和 SSH 密码各有一个眼睛按钮，在掩码和明文之间切换。显示状态只属于这个输入框，面板关掉就复位，不会把密码留在屏幕上。

- **左栏过滤器的角标会把所有生效的过滤条件都数进去。** 标记过滤此前只是把按钮点亮、不作声，于是两个条件同时开着时角标却显示 1。现在状态和标记一起计数，和下拉里打勾的数量对得上；只开一个状态过滤时仍然保留那个彩色圆点。

### 修复

- **macOS 上的自动更新恢复正常。** v0.1.103 的安装包里混进了 `._VelaTerm.app` 这类 AppleDouble 伴生条目，而更新器会去掉每条路径的第一层——这类条目去掉之后就成了空路径——于是它干脆拒绝解包。两个架构都中招，所有用 macOS 的 v0.1.103 用户都卡在了那一版。现在打包不会再写出这些条目。

- **应用主题与系统不一致时，原生控件跟着应用走。** 切换主题只改了应用自己的配色，没有同步 `color-scheme`，而那个值只在启动时按系统偏好种下一次、之后再没变过，于是系统深色 + 应用浅色时，复选框、下拉和滚动条还是深色的。（由 FarhadGSRX 贡献。）

- **干净 clone 下来能编译了。** Rust crate 在编译期就要嵌入 `../dist`，而 dev 命令并不产出这个目录，于是新克隆的仓库还没跑起来就先编译失败。现在构建脚本会在目录缺失时把它建出来。（由 FarhadGSRX 贡献。）

---

## v0.1.103 — 2026-08-24

### 修复

- **Windows 上 Codex 会话不再拒绝启动。** 每次开 Codex 会话都报 `unexpected argument '--codex-hook'` 然后立即失败，原因是生命周期钩子的 TOML 表通过命令行传入时带着空格和双引号，而 npm 安装的 `codex.cmd` 要经过 cmd.exe 重新解析——cmd.exe 会吃掉引号、按空格把值拆成多个参数。现在 Windows 上跳过钩子注入，状态检测退回到已有的 notify / screen / busy 启发式方式，仍然能报告忙闲状态，只是不如钩子精确。macOS 和 Linux 不受影响，继续使用钩子。

- **回滚了 v0.1.102 的 Windows 输入法预编辑修复。** 那个修复在恢复中日韩输入法预编辑浮层的同时，给它加了背景色、1px 边框和圆角，导致打字时终端里会冒出一个小框把拼音圈住——终端里不该出现这种框。由于浮层尺寸、helper 容器的几何计算和 textarea 清理三处互为前提，只能整体退回。Windows 上盲打中日韩的问题仍然存在，跟踪在 issue #6。

---

## v0.1.102 — 2026-08-23

### AI 智能体

- **智能体预设：多个兼容的 CLI 可以并排使用。** 过去每种智能体都写死了一个可执行文件，于是一个分支版本、一个 nightly 构建，或者另一个说着同样协议的 CLI，都没有地方安放——只能去改现有类型的启动参数，改完原来的那个就没了。现在一个预设自带可执行文件、图标和启动参数，并出现在新建会话菜单里，与内置类型并排。会话会记下自己是由哪个预设创建的，因此 Fork 出来的会话仍然用同一个可执行文件；在桌面端创建的预设也会出现在已配对的浏览器和远程客户端上，图标一起带过去——因为图标是以数据的形式传输的，而不是某台机器上的一个路径。已有会话完全不受影响：旧版本留下的数据库，启动后和以前一模一样。

- **子任务确认卡可以挑模型和推理强度，一处回答，处处生效。** 智能体请求派生子任务时，确认卡现在可以选模型，智能体支持的话还能选推理强度，两者都按父会话自己的启动参数预填，因此常见情况点一下就完事。在卡片上换成另一个智能体，两项会重新推导，一个 CLI 的模型名不会再跑到另一个 CLI 的命令行上。这张卡会出现在每个已连接的客户端上，在其中一个上回答，其余的会一起消失；第一个回答还会在服务端认领这个任务，于是在同一秒里用手机和桌面各确认一次，也只会创建一个 worktree 和一个子会话，而不是两个。

### 工作区

- **镜像模式：所有客户端共用同一套布局。** 终端数据流一直是共享的——一个 PTY，一条字节流——但围绕它的那套排布只存在于各个客户端自己的浏览器存储里，因此局域网里打开的浏览器有自己的一套标签和分屏，在一边重新排布，另一边毫无反应。打开镜像模式后，标签、分屏、当前会话、侧边栏选中项和折叠的面板都会发布给每个客户端，并被所有客户端跟随。开关由主机在远程访问面板里控制。在任何一端重新排布，另一端都会跟着变；某个会话离开本窗口的布局时是被分离，而不是被杀掉，因此跟随对端永远不会终结别人的进程；应用对端布局时也不会把键盘从正在本地打字的人手里抢走。手机不参与——手机上是两级导航，是另一种形态的界面，把桌面的分屏树复制过去对谁都没有好处。

- **右栏的 Git 页签做成了能用的 Git 客户端。** 它以前只会列出改动过的文件。现在它可以暂存和取消暂存单个文件或整组文件、放弃更改、写提交（含修改上一次提交），并显示提交历史以及每个提交涉及的文件和差异——分成已暂存、已修改、未跟踪和已提交几组，可以折叠起来。路径统一从仓库根目录处理，因此在子目录里打开的会话，操作的就是它显示的那些文件；处于分离 HEAD 状态时会如实标出，而不是显示一个叫 HEAD 的分支。

### 界面

- **⌘Q 现在问的问题和关窗口一样。** 应用菜单里的退出项原本是系统自带的那个，按下去直接终止进程：按 ⌘Q 会跳过关闭按钮会弹的「保存工作区」确认，同一个意图，换个方式表达行为就不一样。现在两条路都走同一个确认。如果显示确认的那个窗口在此期间重新加载或崩溃了，再按一次 ⌘Q 会重新询问，并回退到原生对话框，而不是让应用退不掉。

- **快捷键提示显示的是真正能用的按键。** 各平台的默认键位本来就不同，而浏览器会把 ⌘/Ctrl 加字母的组合留给自己——⌘D 是收藏，⌘T 是开标签页——所以当 VelaTerm 以网址方式打开时，macOS 上应用自己的 ⌘ 快捷键根本传不到页面。现在普通浏览器标签页在所有操作系统上都改用 Ctrl+Alt 这套键位，桌面应用和远程连接窗口仍然用 ⌘。工具提示和空标签页上的提示会显示当前实际生效的键位，包括你自己改过的，而不是写死的 ⌘ 组合；终端拦截的也正好是应用已经占用的那些组合，所以给某个操作换键，拦截也跟着换过去。

- **字体预设覆盖了 Nerd Font 和中日韩字体，自己填的字体没装会明说。** 预设列表加入了常见的 Nerd Font 和中日韩字族；手动输入的字体会回显并检查一遍：系统里没有这个字体，设置页会直接说出来，而不是悄悄回退到一个和你要的完全不像的默认字体。

- **macOS 上的输入框不再自动大写或自动更正。** 系统的自动首字母大写、自动更正和拼写检查过去对应用里的每一个输入框都生效，会话名和命令输入框也不例外，于是「npm」被改成了「Npm」。现在这些在所有地方都关掉了。

### Windows

- **输入中文、日文、韩文时，预编辑文字和候选词窗口又能看见了。** 这两样此前都是隐形的——只能盲打，按下回车才看到结果。原因出在我们自己的两条 CSS 规则上：承载预编辑浮层的容器宽度塌成了 0，浮层的 `right` 偏移在这个容器里算下来什么都不是。候选词窗口跟着一起消失，因为操作系统是按浮层的矩形来定位它的。现在浮层重新画出来了，配色也用上了应用自己的；承载它的那个隐形输入元素在组词结束后会释放自己占据的区域，因此在那一块上点击和拖动能落到终端上，而不是被一个一直盖着的空元素挡住。

- **原生标题栏跟随浅色/深色设置。** 应用保留了系统标题栏，而 Windows 在没被告知之前一直把它画成浅色，于是深色界面上方顶着一条白边。现在它会与应用保持一致，后续打开的窗口——比如 SSH 和远程连接窗口——也一样。选择「跟随系统」则把控制权交回操作系统，而不是钉死一个值。

- **冷启动时那个多余的小方块没有了。** 单实例插件会创建一个隐藏的消息窗口，却一直没给它自己样式里承诺的透明属性，于是 Windows 偶尔会把这个零尺寸窗口撑到最小尺寸，在启动过程中画出一个小方块。现在它是真的透明了；单实例行为没有变化。

### 性能

- **远程访问首屏要加载的东西少了很多。** 静态资源现在按需压缩，并带上缓存校验标记，因此第二次访问是重新校验而不是重新下载；语言包和终端的可选渲染器也改成用到时才加载，不再算进首屏的那一份。两项加起来，首次传输量降到原来的五分之一左右。

### 修复

- **派生出来的子会话，现在和请求它的那个会话以同样的方式启动。** 子会话过去既不继承父会话的权限模式，也不继承它的启动参数，于是一个跳过确认在跑的会话，它的子会话起来后反而要你确认，父会话上钉住的模型也丢了。现在两者都会继承，取不到就回退到该智能体类型的全局默认值——和「新建智能体会话」菜单用的是同一份。

- **重连远程窗口不再误报「认证失败」。** 窗口重连时，新的 WebSocket 和它取代的那个会互相竞争；失败一方的拆除被当成认证失败上报，于是横幅指责一个完全有效的配对被拒绝了。

- **分组不能再被拖进自己的子树里。** 把一个分组拖到它自己的某个后代上，会让整条分支从树上脱落，里面的会话从侧边栏消失，直到手工修复数据库才能找回。现在这个移动会被拒绝。

- **从别的工具里启动 VelaTerm，智能体输出的颜色不会丢。** 终端会继承启动它的那个程序的环境，因此从导出了 `NO_COLOR`、`CI` 或 `FORCE_COLOR=0` 的 IDE 或智能体框架里启动，VelaTerm 里的智能体 TUI 会全部渲染成单色，尽管终端本身声明支持全彩。现在会话启动时会丢弃这些继承来的值；你自己在 shell 配置文件里导出的同名变量仍然生效，因为那份配置是在会话内部执行的。

## v0.1.101 — 2026-08-15

### 远程访问

- **可以选择分享链接使用的地址——Tailscale 地址现已显示。** 地址列表过去只接受传统的私有 IPv4 网段,因此像 Tailscale 这样从运营商级 NAT 网段(100.64.0.0/10)分配地址的 VPN 组网,会被静默排除在远程访问面板和配对链接之外——尽管服务器早已可以通过这些地址访问。这些地址现在会列出;VPN 隧道排在最后,永远不会成为默认项。面板中新增的 IP 选择器(启动前和运行中均可见)会列出每个候选地址及其网络接口名称,并标记 VPN 隧道;选择某个地址后,其 URL 会移到最前,配对链接也会以该主机重新生成,因此复制的链接在只能通过 VPN 访问本机的设备上也能直接使用,无需手动修改 URL。配对链接下方的二维码可直接用手机扫描。选择会被记住;如果所选接口消失,面板会回退到“自动”,但不会忘记你的选择。服务器本身保持不变,仍在所有接口上监听。选择一个在服务器启动后才出现的地址(例如稍后才连接的 VPN),现在复制的 URL 和二维码也会立即更新,而不再是只有配对链接跟随、其余要等到下次重启;VPN 隧道在所有平台上都排在 LAN 地址之后,服务器停止时所做的选择会决定启动后的第一个配对链接,重叠的链接重新生成也不会再用旧链接覆盖新链接。

- **共享现在可以在重启后继续使用。** 过去配对令牌在服务器每次启动时都会重新生成：只要退出并重新打开 VelaTerm，所有已分享的链接就会悄悄失效，每部手机都得重新配对。现在，令牌、已配对设备和设备封禁列表都保存在数据目录下一个仅所有者可读的文件里：已配对的设备在重启后仍可通过保存的 URL 直接重连（访问密码仍是必需的第二重验证），被撤销的设备也依然保持撤销状态。VelaTerm 还会记住共享处于开启状态：在服务器运行时退出应用，下次启动会自动在同一端口恢复服务——桌面应用和通过 `--serve` 运行的无界面服务器都是如此；如果是您自己停止的服务器，下次启动则不会自动开启任何东西。即使自动启动失败（例如端口被占用），应用也会正常启动，远程访问面板会显示失败原因。端口输入框现在会记住实际使用过的端口，而不再重置为默认值；「重新生成链接」仍是显式的作废开关：它会立即签发新令牌、让所有旧链接失效并覆盖保存的状态。访问密码本身永远不会写入磁盘——只保存一个内存困难型哈希（Argon2id）。

### 安全

- **已配对设备无法再管理共享本身。** 此前,任何已配对的浏览器都能调用与桌面应用相同的管理命令——创建新的配对链接(这同时会清空设备屏蔽列表)、列出并吊销其他设备、停止或重新配置服务器——而设置存储会把完整的设置表交给每个客户端,其中包括访问密码的内存困难哈希,以及下次启动要读取的自动启动设置。管理命令现在仅限桌面应用和 Electron 外壳使用;设置 API 会在已配对设备的每次读取中过滤远程访问相关键和 Gitea 令牌,并拒绝对它们的写入。已配对设备保留配对的本来用途——终端会话和完整的 shell 访问——但再也无法读取密码校验值、邀请或驱逐其他设备,或篡改下次启动使用的端口。读取、写入或删除已存储机密的命令——Gitea 令牌和已记住的主机密码——现在对已配对设备同样会被拒绝;接收路径的命令——读取、预览、写入、创建、重命名和删除,以及显示文件的 git 差异或选择克隆仓库的目标文件夹——会先解析符号链接，再拒绝位于 VelaTerm 自身数据目录内的路径(配对状态和密钥就存放在那里),其他所有路径照常可用,因此远程文件浏览和编辑不受影响。一项测试会枚举每个接收路径的远程命令,新命令无法悄悄绕过这项检查。当这些保护拒绝某个请求时,浏览器现在会显示经过翻译的提示,而不是原始的英文错误。

- **吊销设备或重新生成链接,在双实例配置下也不会再丢失。** 在开启自动启动的无界面 `--serve` 服务器上,两个服务器实例各自持有保存的配对状态副本并整体回写:通过一个实例完成的吊销或新配对链接,可能被另一个实例悄悄撤销。同一进程内的所有实例现在按数据目录共享单一配对状态:吊销与轮换立即在所有位置生效,文件只由一处写入,并且在真正的重启之间仍是权威数据来源。

- **反复失败的登录会被限速。** 访问密码的校验使用 Argon2id,代价刻意高昂——而任何能连到端口的人都可以尝试。同一地址失败五次后,后续尝试会在任何哈希计算开始之前被拒绝一分钟;哈希计算本身现在也在服务器事件循环之外执行,并对并发校验数量设有硬性上限:大量错误密码既不能再用内存困难哈希拖垮服务器,也不能拖慢已连接设备的响应。该限速器只存在于内存中,随服务器重启而重置;真正的屏障仍是配对令牌和密码。该限制现在由使用同一数据目录的所有服务器实例共享——`--serve` 双实例配置不再使尝试次数翻倍;并且在密码校验开始前就会预留一次尝试,使来自同一地址的并行请求无法绕过限制。被限流的浏览器现在会在登录界面看到专门的限流提示,而不是被告知密码错误;限流也不再像密码错误那样被记住:等待结束后,下一次尝试无需刷新页面即可再次通过。中途放弃的尝试——密码还在校验时标签页就被关闭——现在会立即释放其预留的名额,而不是在这一分钟剩下的时间里继续计入该地址;登录成功也只释放自己的预留,而不再清空该地址的全部记录:在共享网络地址背后,某人正确登录不再重置攻击者的尝试额度,已记录的失败只随其所在的一分钟过期。

- **磁盘上和日志中的机密得到更谨慎的处理。** 保存配对状态的文件和端到端加密密钥现在从创建那一刻起就仅所有者可读,而不是先写入再收紧权限;存有密码哈希的会话数据库同样仅限所有者访问。无界面 `--serve` 不再把配对链接中长期有效的机密打印到日志:当输出不是终端时,链接会被隐去,只显示一条提示;`--print-pairing` 可显式恢复原行为。设备注册表上限为 32 条,名称长度受限,已配对客户端无法让保存文件无限膨胀;保存吊销或新链接失败时,错误现在会返回给调用方,而不是只留在日志里。自动启动不再顶替已手动启动的服务器,过期的自动启动错误在您自行停止服务器后即会消失。

### 修复

- **现在也可以在 Electron 外壳中管理配对。** 生成配对链接、列出已配对设备和吊销设备此前只作为桌面（Tauri）命令存在；Electron 外壳和浏览器客户端使用的 WebSocket 分发器会返回 "Unknown command"，导致远程访问面板在那里无法使用。现在这三个命令在两条传输通道上都经过同一套核心函数，两者不会再产生偏差；回归测试覆盖了新的分发路径，包括对一个正在运行的本地服务器实际生成配对链接。

## v0.1.100 — 2026-08-10

### AI 智能体

- **Kiro CLI 成为一等公民会话类型。** Kiro 会话在树里有自己的节点，状态点由 Kiro 自己的生命周期 hooks 驱动，如实反映工作中与等待处理；一个回合结束会发通知；重新打开节点自动接回同一段对话；启动参数、跳过确认开关、经 vspawn 派生子会话也都齐了——其它智能体已有的能力，Kiro 现在都有。VelaTerm 把你的默认 Kiro agent 克隆成自己的 `vlx-term` agent，只在副本上加一份只观察、不干预的生命周期 hooks 再启动它，你自己的 agent 文件全程不动，提示词、工具和 MCP 服务器原样带过来。Kiro 没有权限请求 hook，所以它等你批准的时候，状态点仍然显示为工作中。

### 修复

- **终端里启动的程序不再继承 AppImage 自己的环境（Linux）。** AppImage 的启动器会把 `PYTHONHOME`、`PYTHONPATH`、`PERLLIB`、`QT_PLUGIN_PATH` 和 GStreamer 的插件路径指向包内的临时挂载目录，并把包内目录插到 `PATH`、`LD_LIBRARY_PATH` 的最前面。终端会把整个环境交给它启动的 shell，于是系统的 `python3` 跑到包里去找标准库，根本起不来；其它动态链接的程序也会优先加载包里的那份库，而不是系统的。现在 VelaTerm 在启动 shell 或外部工具之前先把这些包内路径剥掉，你自己设的值保持不动。`APPDIR` 和 `APPIMAGE` 仍然可见，靠它们判断自己是不是从 AppImage 启动的程序照样拿得到答案。只有 AppImage 版本受影响，deb 包、macOS 和 Windows 行为不变。

## v0.1.99 — 2026-08-09

### 终端

- **Shift+Enter 换行，不再直接提交。** 终端协议里没有「带修饰键的 Enter」这种编码，因此 Claude Code、Codex 这类智能体命令行收到的只是一个普通回车，话还没写完就被发了出去。现在 VelaTerm 改发 ESC+CR，与这些工具在 iTerm2 上配置键位映射后期望的序列一致，多行输入终于可用——macOS 也包含在内，此前那里根本没有安装自定义按键处理。输入法组词期间不受影响，Enter 仍用于上屏候选词。

### 项目与组织

- **单个会话的状态可以单独刷新。** 在启用了状态筛选的窗格中，会话菜单新增「刷新状态」，只按该窗格自己的条件重新判断这一个会话并决定去留，其余会话原地不动。该操作归属于菜单所在的窗格，因此递归切分时不会误用其它窗格的筛选条件；结果按窗格分别保存，重启后恢复。
- **清除标记只需点一下。** 再次选择已生效的表情即可取消，原先单列的清除项和分隔线随之移除。筛选按钮上的表情徽章也一并去掉：按钮高亮已经说明表情筛选处于启用状态，具体是哪个表情菜单里写着。

### 修复

- **Linux AppImage 的桌面集成在任何机器上都能安装。** 打包进去的图标是一个指向构建机绝对路径的符号链接，因此 Gear Lever、AppImageLauncher 这类工具无法提取图标，尽管程序本身运行正常。现在改为相对链接。glibc 要求也更正为 2.35——此前只测了主程序，未计入随包分发的库——因此桌面端支持的最老发行版为 Ubuntu 22.04。

## v0.1.98 — 2026-08-02

### AI 智能体

- **Grok Build 成为 VelaTerm 的一等智能体。** 现在可以安装、启动和续接 Grok 4.5；稳定的会话 ID、官方 lifecycle hooks、准确的工作与权限状态、合并后的转录、用量详情和随主题变化的官方图标，在桌面端、浏览器端和移动端保持一致。

### 项目与组织

- **把项目左栏切分成彼此独立的工作视图。** 任一树窗格都能继续向下切分，并在重启后恢复各自的搜索、状态与表情筛选、折叠状态和分隔比例。所有窗格仍是同一份后端项目树的投影视图，编辑会同步生效，不会复制业务数据。
- **标记并筛选节点时不再丢失上下文。** 项目、分组和会话都可以添加表情标记。命中标记的容器会保留完整子树；状态成员在操作期间保持稳定，同时支持动态增加和手动刷新；状态与表情条件按并集合并。
- **直接创建空项目。** 选择父目录并校验名称后，可在同一流程中创建并导入文件夹。中途导入失败时只会重试导入，不会重复创建目录。

### 界面

- **在社区常用的平台分享 VelaTerm。** 分享弹窗现在支持微信朋友圈、微博、小红书、X、Reddit、Hacker News、LinkedIn、Facebook、Telegram 和 WhatsApp；微信提供二维码流程，更新弹窗中也新增了分享引导。
- **细节交互更顺手。** 临时终端标签在保存为正式会话前即可重命名；普通输入框会关闭移动键盘的自动首字母大写，同时不改变终端输入行为。

## v0.1.97 — 2026-07-25

### AI 智能体

- **会话不会再卡在“工作中”。** Codex 通过各自独立的短命进程上报工具活动和回合结束，回调到达顺序可能颠倒，导致已经结束的回合仍显示为工作中。现在会丢弃在自身回合结束之后才到达的中途上报，并新增会话结束 hook，覆盖没有发出完成事件就退出的会话。
- **被打断的回合几秒内即可恢复正常。** 按下 Esc 或遇到流式错误时，Claude 与 Codex 的回合结束时完全不会发出完成回调。现在终端连续静默六秒后会安静地把这类会话校正为等待中，且不会弹出“已回复”通知。

### 界面

- **macOS 上的分屏快捷键更可靠。** 向右分屏（Cmd+D）和向下分屏（Cmd+Shift+D）现在同时注册为原生 Terminal 菜单命令，macOS 不会再在 VelaTerm 收到之前拦截这组按键。
- **一次按键只保存一次文档。** Cmd+S 之前会被全局快捷键和聚焦中的编辑器同时处理，可能在一次按键中把同一个文件写入两次。

## v0.1.96 — 2026-07-23

### AI 智能体

- **Codex 状态只信任 lifecycle hooks，不再依赖终端猜测。** 新版 Codex 会话现在只使用官方 lifecycle hooks 作为活动状态来源。`SessionStart` 握手会验证回调链路；缺少回调时明确显示“状态不可用”；终端文字或输出活动也不会再覆盖工作、待确认或已完成状态。
- **每个回合结束后更及时地刷新 Codex 用量。** Info 面板会立即显示本地 rollout 快照，再与实时速率额度校准；Codex 写完最终 token 快照后还会补一次刷新，并忽略来自旧会话的延迟响应。

### 界面

- **macOS 上的项目树命中更可靠。** 虚拟行不再依赖合成器 transform，避免滚动或树更新后 WKWebView 保留过期命中坐标，导致悬停、点击或拖拽落到另一行。

## v0.1.95 — 2026-07-21

### AI 智能体

- **Kimi Code 和 Zoo Code 加入会话树。** VelaTerm 现在可以启动、续接、安装并配置这两种智能体。Kimi 使用官方生命周期 hooks 权威上报工作、权限和等待状态；Zoo Code 保持稳定任务标识，并在缺少外部 hooks 时使用终端检测。
- **实时刷新 Codex 用量。** Info 面板现在可以主动查询 Codex app server 获取最新速率额度，同时保留本地 rollout 快照作为兼容回退。

### 项目与终端

- **使用 `vela <path>` 打开项目。** 发布版现在可以安装类似 VS Code 的 shell 命令；再次调用会把项目转交给已有 VelaTerm 窗口，而不是打开重复实例。
- **可见、可取消的 Git 克隆。** “克隆项目”现在显示 Git 阶段、百分比与耗时，可提示长时间无进展，并能取消整个 Git 进程树且不留下半成品目录。界面错误和审计日志会隐藏凭据与查询 token。
- **Windows 上的 WSL 终端。** 所有已安装的 WSL 发行版都会被探测，并与 PowerShell、cmd、Git Bash 一起供普通终端选择。智能体会话仍使用 Windows 宿主 shell，确保 hooks 与可执行路径可靠。

### 界面与可靠性

- **更清晰的后台会话控制。** 后台标签菜单会显示每个会话的实时状态，超限弹窗也可以一次结束多个已选标签。
- **更安全的应用生命周期与更新说明。** 退出前会确认是否停止运行中的会话；Codex lifecycle 的精确身份优先于含糊的 rollout 扫描；自动更新说明支持所有内置界面语言。

## v0.1.94 — 2026-07-12

### 本地化

- **越南语界面。** 现在可以在语言选择器中选择 Tiếng Việt，使用越南语系统区域设置时还会自动选中。

### 浏览器

- **内置浏览器启动更快。** 现在，每个浏览器标签都提供一键打开 ChatGPT、Claude、Gemini 和 Google 的快捷入口。项目和分组的上下文菜单也可以直接在会话树的相应位置创建固定浏览器页面。

### 图片和文档

- **在 macOS 上可靠地粘贴图片路径。** 当 WebKit 没有把复制的图片暴露为文件时，VelaTerm 现在会从系统剪贴板读取图片，并仍将其作为文件路径上传，而不会静默退回智能体原生的图片占位符。远程窗口会始终显示图片粘贴设置、解释为何必须使用文件路径模式，并禁用不可用的原生选项。
- **将图片粘贴到源文档。** 源代码编辑器现在支持粘贴剪贴板图片。已保存的 Markdown 文档会把图片存放在文档旁的 `assets/` 目录中，并插入可移植的 Markdown 图片语法；未保存的草稿会嵌入图片数据，避免临时文件清理时丢失。

### 界面

- **上下文菜单始终可见并准确定位。** 在右侧边缘附近打开的菜单会被正确测量和调整位置。右键单击树节点时，现在只会高亮菜单目标而不改变已有选择；分组菜单中也新增了作用于该分组的终端入口。
- **更清爽的编辑效果和状态标签。** 源代码文本不再为 HTML 注释等字符序列渲染箭头状字体连字；用量百分比会明确标注为已使用；宿主 WebView 无关的原生上下文菜单也不会再出现在 VelaTerm 自有菜单后方。

### 修复

- **Codex 保持在普通终端历史记录中。** 由 VelaTerm 启动的 Codex 会话现在使用内联终端模式，因此按 Esc 中断或返回时，不会再切换终端屏幕缓冲区并让回滚视图跳到顶部。用户自己的 Codex 配置不会被修改。
